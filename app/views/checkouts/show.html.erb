<p id="notice"><%= notice %></p>

<div class="container mt-5 mb-5">
  <div class="row d-flex justify-content-center">
   <div class="col-md-8">
    <div class="card">
      <div class="text-left logo p-2 px-5"> 
        <img src="https://i.imgur.com/2zDU056.png" width="50"> 
      </div>
    <div class="invoice p-5">
    <h5>Your order Confirmed!</h5> 
    <span class="font-weight-bold d-block mt-4">Dear Customer,</span> <span>You order has been confirmed and will be shipped in next two days!</span>
    <div class="payment border-top mt-3 mb-3 border-bottom table-responsive">
     <table class="table table-borderless">
      <tbody>
      <tr>
        <!-- <td>
        <div class="py-2"> <span class="d-block text-muted">Order Date</span> <span>   <%#<% Date.today %></span> </div>
        </td> -->
      <td>
        <div class="py-2"> <span class="d-block text-muted">Customer Name</span> <span><%= @checkout.firstname %>
          </span><span><%= @checkout.lastname %></span> </div>
      </td>
      <td>
        <div class="py-2"> <span class="d-block text-muted">Customer Email</span> <span><%= @checkout.email %>
        </span></div>
      </td>
      <td>
        <div class="py-2"> <span class="d-block text-muted">Contact</span> <span><%= @checkout.contact %></span> 
        </div>
      </td>
      <td>
        <div class="py-2"> <span class="d-block text-muted">Shiping Address</span> <span><%= @checkout.address1 %></span><span><%= @checkout.address2 %></span></div>
      </td>
     </tr>
     </tbody>
    </table>
   </div>
   <div class="product border-bottom table-responsive">
    <table class="table table-borderless">
      <tbody>

      <section class="section">
        <%= render(@cart.line_items) %>
          <div class="column total has-text-right">
            <h4 class="title is-4">
            <span class="f5 has-text-grey">Total:</span> <%= number_to_currency(@cart.total_price) %>
            </h4>
         </div>
      </section>

      </tbody>
    </table>
  </div>

   <p>We will be sending shipping confirmation email when the item shipped successfully!</p>
   <p class="font-weight-bold mb-0">Thanks for shopping with us!</p> <span>EMusic Team</span>
 </div>
 <div class="d-flex justify-content-between footer p-3"> <span>Need Help? visit our <span>support@emusic.com</span> help center</a></span> </div>
 </div>
 </div>
</div>
</div> 

<%#<%= link_to 'Edit', edit_checkout_path(@checkout) %> 
<%#<%= link_to 'Back', checkouts_path %>
